# Vite / npm
manuscript-reader/node_modules/
manuscript-reader/dist/
# Stale build output from the pre-React migration once lived at the repo root.
# The live build is manuscript-reader/dist (Netlify base dir); never track dist.
/dist/
/package-lock.json

# Environment files
.env
.env.local

# macOS
.DS_Store

# Logs
npm-debug.log*

# Real manuscripts used as test fixtures — unpublished work, do not publish.
# The synthetic .md fixtures and check-fixtures.mjs ARE tracked on purpose.
manuscript-reader/fixtures/*.docx